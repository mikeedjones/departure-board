"""Wi-Fi credentials for this network.

Never committed — see .gitignore's `firmware/config.py` rule.
Fill in the two values below with your home network's details before
deploying. They stay on the device's filesystem only.
"""

WIFI_SSID = "your-network-name"
WIFI_PASSWORD = "your-password"
